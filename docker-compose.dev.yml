services:
  pocketbase:
    build: ./infra/pocketbase
    container_name: 00-vitehono-pb-dev
    ports:
      - "127.0.0.1:${PB_PORT:-9000}:8090"
    volumes:
      - ./pb_data:/pb/pb_data
      - ./infra/pocketbase/pb_migrations:/pb/pb_migrations
    restart: unless-stopped
